# Step 11 — File Manifest

Total files in ZIP: 173

Key structure:
- `.github/` — CODEOWNERS, PR template, CI workflow
- `.husky/` — pre-commit, pre-push, commit-msg hooks
- `client/src/` — App.tsx, test-setup, app-v2/, features-v2/
- `commitlint.config.mjs` — commit message validation
- `docs/` — governance, architecture, ADRs, review reports (step-01 through step-11)
- `eslint.config.js` — linting configuration
- `package.json` — scripts, dependencies, lint-staged
- `scripts/` — 25+ guard/scaffold scripts + 6 test files
- `server/` — index.ts, domains-v2/ scaffolds
- `shared/` — shared types
- Config files: tsconfig, vite, vitest
