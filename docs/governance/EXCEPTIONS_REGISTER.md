# PlatformaX V2 — Exceptions Register

Status: `ACTIVE`
Owner: Governance

## Purpose

Registry of active exceptions to governance rules. Every exception must be justified, time-bound, and reviewed.

## Format

| Field | Description |
|---|---|
| Exception ID | Unique identifier (EXC-NNN) |
| Rule ID | Which rule is being excepted |
| Reason | Why the exception is needed |
| Expiry | When this exception expires or must be reviewed |
| Owner | Who approved the exception |
| Evidence | Link to ADR, report, or approval |
| Risk | What risk this exception introduces |
| Status | active / expired / revoked |

## Active Exceptions

| Exception ID | Rule ID | Reason | Expiry | Owner | Evidence | Risk | Status | Files |
|---|---|---|---|---|---|---|---|---|
| EXC-001 | PX-CODE-001 | Multi-step onboarding flow holds linear form state for 5 steps; splitting per-step into separate route shells would fragment shared state and submit/validation control flow without test-coverage payback while the runtime is still PARTIAL. | 2026-08-31 | dawid | docs/review/step-30-architecture-quality-scalability-guards/STEP_30_REVIEW.md | Component grows past 280-line route limit, harder to review; mitigated by extracted step components and form-state hook. | active | client/src/app-v2/onboarding/OnboardingFlow.tsx |
| EXC-002 | PX-CODE-001 | Profile header CSS is a single responsive composition with 6 breakpoint blocks (mobile-first → desktop → reduced-motion); CSS modules cannot import each other, so splitting would duplicate selectors and break cascade. | 2026-08-31 | dawid | docs/review/step-30-architecture-quality-scalability-guards/STEP_30_REVIEW.md | One CSS module exceeds the 320-line / size cap; mitigated by per-block comment headers and design-token usage. | active | client/src/app-v2/profile/styles/profile-header.module.css |
| EXC-003 | PX-SEC-001 | Shared contract files co-locate the owner-only (Private) Input/Owner types — CompleteOnboardingInput / UpdatePrivateProfileInput / UpdatePersonalStatusInput / OwnerProfileView — with their Public counterparts. PII tokens (`phone`, `dateOfBirth`) appear in the Private types and the file-level PII guard cannot tell the surrounding type's privacy class. Public DTOs (PublicProfileDTO / PublicProfileView) are filtered by the server-side mapper and the no-PII property tests in identity domain still cover them. | 2026-08-31 | dawid | server/domains-v2/identity/__tests__/public-mapper-no-pii.test.ts and server/application-v2/use-cases/profile/__tests__/service.test.ts (PII-free public view assertions) | Marker may grow stale if a future contributor adds a new Public type to the same file without realising PII is allowlisted at file scope; mitigated by the dedicated no-PII property tests on the public mapper + public view. | active | shared/contracts/identity.ts shared/contracts/profile.ts |
| EXC-004 | PX-SEC-001 | Contacts contract and identity contact-access DTOs co-locate owner-only types (`OwnerContactFieldsDTO`, `UpdateContactFieldsInput`, etc.) and the policy-filtered `VisibleContactFieldsDTO` with the public `ContactProfileRelationshipDTO`. PII tokens (`phone`, `emailContact`) appear in the owner/policy-filtered types; the file-level PII guard cannot tell the surrounding type's privacy class. The policy filter lives in `server/domains-v2/identity/contact-access-policy.ts` + `application-v2/use-cases/contacts` and is covered by `contact-access-policy.test.ts`, `contact-access-service.test.ts` (zero-PII for stranger), and the application-level public-DTO-zero-PII tests. `ContactProfileRelationshipDTO` itself carries no raw PII — only the already-filtered `visibleContactFields` map produced by the policy. | 2026-08-31 | dawid | server/domains-v2/identity/__tests__/contact-access-policy.test.ts + contact-access-service.test.ts + server/application-v2/use-cases/contacts/__tests__ — public-DTO-zero-PII and viewer-policy tests. | Marker may grow stale if a future contributor adds a new Public type to one of these files without realising PII is allowlisted at file scope; mitigated by the dedicated policy tests covering owner / friend / approved / stranger combinations. | active | shared/contracts/contacts.ts server/domains-v2/identity/contact-access-dto.ts |
| EXC-005 | PX-CODE-001 | `shared/contracts/contacts.ts` is the single canonical contract for the whole Kontakty slice: field/permission types, contact-request lifecycle, the four owner-vs-mutual relation concepts, owner-local circle labels (`FriendCircle`, `ContactGroupEntry`) and the view DTOs (`ContactProfileRelationshipDTO`, `ContactListItemDTO`, `ContactsTabData`). The owner clarification (2026-05-29) added the circle/list types, pushing it to 20 type exports (budget 15). Splitting would fragment the source of truth and create circular type imports between the view DTOs and the relation enums. | 2026-08-31 | dawid | docs/review/contacts-v2/LEGACY_CONTACTS_ANALYSIS.md + server/application-v2/use-cases/contacts/__tests__/service.test.ts (owner-logic separation tests). | Type-only contract file exceeds the 15-export structural budget; harder to scan. Mitigated: it is types-only (no logic), grouped by clearly commented concept blocks, and covered by the use-case owner-logic tests. | active | shared/contracts/contacts.ts |
| EXC-006 | PX-CODE-001 | Friend-posts (Slice 11) foundation submodule keeps its DTO surface (`dto.ts`), service orchestration (`service.ts`) and content-v2 aggregated facade (`public-api.ts`) in single files until the API stabilizes past the foundation slice; splitting would fragment the canonical contract surface. Pre-existing markers already present in those files when Slice 12 reached `check-inline-exceptions-registered`. | 2026-08-31 | dawid | server/domains-v2/content-v2/friend-posts/__tests__/friend-posts-service.test.ts pinning the public-api shape + visibility behavior. | DTO + service + content-v2 facade exceed structural budgets; harder to scan. Mitigated by the friend-posts service tests + the architecture/import-graph guards. | active | server/domains-v2/content-v2/friend-posts/dto.ts server/domains-v2/content-v2/friend-posts/service.ts server/domains-v2/content-v2/public-api.ts |
| EXC-007 | PX-CODE-001 | Slice 12 workplaces foundation keeps `identity/workplaces/dto.ts` (workplace + contact + viewer-state + commands + constants — 19 exports), `identity/workplaces/service.ts` (create/update/archive/list/contact/viewer-state in one orchestration), `professional-profile/types.ts` (UI workplace + post + teaser view types), `professional-profile/WorkplaceWizard.tsx` (5-step creator) and `professional-profile/Workplace.module.css` (shared design tokens for workplace page + wizard + professional layer) in single files until the API + visual surface stabilize past the foundation slice. | 2026-08-31 | dawid | docs/review/professional-profile-v2/WORKPLACES_SLICE_12_REPORT.md + workplaces-service.test.ts + workplace-posts-service.test.ts + workplace-teasers-service.test.ts + workplace-feed-use-case.test.ts + 4 UI tests. | DTO/service/types/CSS/wizard exceed structural budgets; harder to scan. Mitigated by behavior tests + the privacy/security tests pinning no-PII in public DTO + contact-policy gating. | active | client/src/features-v2/professional-profile/Workplace.module.css client/src/features-v2/professional-profile/WorkplaceWizard.tsx client/src/features-v2/professional-profile/types.ts server/domains-v2/identity/workplaces/dto.ts server/domains-v2/identity/workplaces/service.ts |
| EXC-008 | PX-CODE-001 | Slice 12 added the friend-feed "workplace mini-teasers" list section to the existing `FriendFeedPage` thin orchestrator (header + composer + posts list). The page is 147 lines (limit 140); splitting per-section would dilute the page header context for a page that is already a thin shell over the friend-feed mock adapter. | 2026-08-31 | dawid | client/src/features-v2/friend-feed/__tests__/FriendFeedPage.test.tsx + FriendFeedWorkplaceTeaserCard.test.tsx pin the rendering of both posts and teasers. | Friend-feed page grew past the 140-line component limit; harder to skim. Mitigated by per-section helper components (`FriendFeedPostCard`, `FriendFeedWorkplaceTeaserCard`). | active | client/src/features-v2/friend-feed/FriendFeedPage.tsx |
| EXC-009 | PX-CODE-001 | Slice 18 media upload & asset pipeline introduces 21 media purposes across 8 owner types and the generic upload-intent / complete-upload flow. The canonical contract (`shared/contracts/media.ts`), the server service factory (`server/domains-v2/media/service.ts`), the repository surface (`server/domains-v2/media/repository.ts`) and the mock client adapter (`client/src/features-v2/media/media-adapter.ts`) co-locate the related types/factories/implementations as single sources of truth. Splitting would either create circular contract imports (purpose ↔ owner type ↔ adapter input) or fragment shared closures (in-memory Map state in the mock adapter, validation/variant helpers in service). | 2026-08-31 | dawid | docs/review/media-v2/MEDIA_SLICE_18_UPLOAD_ASSET_PIPELINE_REPORT.md + server/domains-v2/media/__tests__ (service + purpose-registry + policy + validation + public-mapper-no-leak) + server/application-v2/use-cases/media/__tests__/service.test.ts + client/src/features-v2/media/__tests__ (adapter + validation + variant resolver). | Contract / service / repository / adapter files exceed structural budgets (33 contract exports, 443-line service, 334-line repository, 122-line mock factory); harder to scan. Mitigated by behavior tests pinning each surface flow + the PII/leak property tests on the public mapper. Planned follow-up: split adapter contract into `shared/contracts/media-adapter.ts` and extract media use-cases into `internal/use-cases.ts` once the live storage backend lands. | active | shared/contracts/media.ts server/domains-v2/media/service.ts server/domains-v2/media/repository.ts client/src/features-v2/media/media-adapter.ts |
| EXC-010 | PX-CODE-001 | Slice 19 friendship + blocking foundation co-locates the friend-request lifecycle (send / cancel / accept / reject / remove), the symmetric block state (block / unblock with pending-request cancellation), and the read-side (listFriends, pending lists, relationship state) in `server/domains-v2/social/service.ts` so the in-memory mock + future repository adapter share one closure over `repository / clock / createId`. Splitting per-action now would fragment the response-shape contract and force three separate factories before transport/runtime stabilizes; would also duplicate `activeBlocksBetween` / `updatePendingBetweenTo` helpers. | 2026-08-31 | dawid | docs/review/social-v2/SOCIAL_SLICE_19_FRIENDS_CONTACT_CONSENTS_REPORT.md + server/domains-v2/social/__tests__/service.test.ts (lifecycle pins). | service.ts exceeds 240-line backend-service budget (378 lines); harder to scan. Mitigated by behavior tests pinning each action + the policy / mapper extraction (`./policy`, `./mapper`, `./repository`) so the factory body is orchestration only. | active | server/domains-v2/social/service.ts |
| EXC-011 | PX-CODE-002 | Slice 19 extended three orchestration/service files past the 400-line complex-service budget while wiring the friendship + contact-consent foundation: `application-v2/use-cases/contacts/service.ts` added `cancelContactRequest` / `revokeContactAccess` / `listContactAccessRequestsForOwner` / `getContactVisibilityForViewer` (470 lines); `application-v2/use-cases/friend-feed/service.ts` added friend-aware visibility (`getFriendIdsForViewer` + `friends_only` filtering, 431 lines); `domains-v2/social/social-contacts-service.ts` added `blockUser` with pending-request cancellation + friend-circle owner-local labels (511 lines). Splitting per-flow now would force callers to wire several smaller factories before transport/runtime stabilizes and would duplicate the deps closure (`identityContactAccess` / `socialContacts` / `friendRequests`). | 2026-08-31 | dawid | docs/review/social-v2/SOCIAL_SLICE_19_FRIENDS_CONTACT_CONSENTS_REPORT.md + server/domains-v2/social/__tests__/service.test.ts + server/domains-v2/social/__tests__/social-contacts-service.test.ts + server/application-v2/use-cases/friend-feed/__tests__/friend-feed-use-case.test.ts. | Three orchestration files exceed the 400-line complex-service budget; harder to scan. Mitigated by behavior tests pinning each new flow + the policy/mapper extraction so the factory bodies stay orchestration-only. Planned follow-up: once the live transport lands, split contacts service into per-flow modules and extract the social block flow into a separate file. | active | server/application-v2/use-cases/contacts/service.ts server/application-v2/use-cases/friend-feed/service.ts server/domains-v2/social/social-contacts-service.ts |
| EXC-013 | PX-CODE-001 | Slice 20B-21 appended ComposerTrigger + ComposerModal styles to `client/src/features-v2/publishing/Publishing.module.css` so the trigger / modal stays inside the publishing-feature cascade (CSS modules cannot import each other; pulling the styles into a sibling file would either duplicate the trigger/modal palette or break cascade with the existing composer body styles). | 2026-08-31 | dawid | docs/review/ui-v2/UI_POLISH_SLICE_20B_21_GLOBAL_CARDS_AND_COMPOSER_REPORT.md + client/src/features-v2/publishing/__tests__ (composer flow tests already pin behavior). | Publishing CSS module exceeds 320-line module budget (346 lines after trigger/modal additions). Mitigated by per-section comment headers + the publishing tests pinning render of composer + trigger. Planned follow-up: once the trigger/modal pattern stabilizes across multiple variants, extract a shared `composer-trigger.module.css` and re-import via inline class composition. | active | client/src/features-v2/publishing/Publishing.module.css |
| EXC-014 | PX-CODE-001 | Slice 20B-FIX top-tier redesign co-locates four feature-canonical stylesheets that each carry the whole feature cascade (CSS modules cannot import each other): (a) `client/src/features-v2/channels/Channels.module.css` (≈510 lines) — ChannelsShell hero + section + ChannelCard grid + ChannelProfileShell + ChannelLeadsPanel + ChannelPostComposer + ChannelPostCard + form/state palette; (b) `client/src/features-v2/communities-v2/feeds/Feeds.module.css` (≈438 lines) — Feeds hero + segmented tabs + composer-inside-modal + scope selector + descendant picker + feed list + post-card + state surfaces; (c) `client/src/features-v2/notifications-v2/NotificationsPage.module.css` (≈393 lines) — Activity Center header + filter chips + per-category icon palette + card unread-accent + actions + empty/loading/error; (d) `client/src/features-v2/content-display/ContentDisplay.module.css` (≈412 lines after FB-style pass) — Post Display Kit card layout + header / body / stats / equal-width action bar / media grid / 10+ variant accents (Friend, Community, Relational, Staff, Channel, Workplace, WorkplaceTeaser, ImportantEvent, ProfilePresentation, Compact) + states. Splitting per-section would either duplicate the palette/tokens or break cascade (e.g. shared `.card` / `.composer` / `.hero` selectors that depend on adjacent state). | 2026-08-31 | dawid | docs/review/ui-v2/UI_POLISH_SLICE_20B_FIX_TOP_TIER_REDESIGN_REPORT.md + existing channels / communities-v2 / notifications-v2 / content-display test suites (already pin rendering and interactions). | Four CSS modules exceed the 320-line module budget. Mitigated by per-section comment headers, global-token usage (no hardcoded palette), and the existing render/interaction tests. Planned follow-up: once the cards/composer patterns stabilise across all features, extract a shared `card.module.css` + `composer.module.css` and re-import via class composition (or move to a CSS-in-JS layer if introduced). | active | client/src/features-v2/channels/Channels.module.css client/src/features-v2/communities-v2/feeds/Feeds.module.css client/src/features-v2/notifications-v2/NotificationsPage.module.css client/src/features-v2/content-display/ContentDisplay.module.css |
| EXC-015 | PX-SEC-001 | Slice 21 Manage Dashboard DTO uses the literals "email" and "phone" as enum values in `ContactSection.fieldsAvailable` (a readonly array of two string literals) to advertise which contact-field categories exist for the owner — not as field carriers. The orchestrator masks the real e-mail (`accountEmailMasked`) and never returns raw phone digits anywhere in the DTO; this is asserted by `manage-service.test.ts > does not leak PII (phone/raw email)` and `ManageDashboardPage.test.tsx > never renders raw e-mail address`. The file-level PII guard cannot distinguish "enum literal" from "field carrier" so the `ALLOW_PRIVATE_DTO_PII` marker is needed. | 2026-08-31 | dawid | docs/review/manage-v2/MANAGE_SLICE_21_ACCOUNT_PRIVACY_SETTINGS_CENTER_REPORT.md + server/application-v2/use-cases/manage/__tests__/manage-service.test.ts + client/src/features-v2/manage/__tests__/ManageDashboardPage.test.tsx (PII-zero assertions). | Marker may grow stale if a future contributor adds a real PII-carrying field (e.g. `emailValue: string`) to the same file; mitigated by the dedicated no-PII tests on the DTO + the masking helper in `service.ts#maskEmail`. | active | shared/contracts/manage-dashboard.ts shared/contracts/manage-dashboard-sections.ts |
| EXC-012 | PX-CODE-001 | Slice 20 moderation foundation co-locates several surfaces. Backend: (a) `server/domains-v2/moderation/contracts.ts` is the single source of truth for the cross-domain moderation enums + registries (target type, reason, severity, status, action, per-target capability rows, per-reason definition rows — 16 exports vs 15-budget). Splitting would either fragment the source of truth across 4–5 files or create cyclic enum imports between dto / events / service. (b) `server/domains-v2/moderation/service.ts` (338 lines) co-locates the create-report flow with the moderator review/list/details/action flow in a single factory so the in-memory mock + future repository adapter share one closure over repository / publisher / clock / createId. Frontend: (c) `client/src/features-v2/moderation/types.ts` (19 exports) is the single source of truth for the UI moderation contract — splitting would fragment dialog ↔ queue ↔ adapter types. (d) `client/src/features-v2/moderation/ReportDialog.tsx` (161 lines) co-locates reason picker + description validation + submit state + success/error banners so dialog lifecycle stays in one closure. (e) `client/src/features-v2/moderation/ModerationQueuePage.tsx` (171 lines) co-locates filter bar + list + detail panel + action bar so the selected-report + refresh closure stays in one place. (f) `client/src/features-v2/moderation/Moderation.module.css` (336 lines) co-locates dialog + queue + per-status chip palette; CSS modules cannot import each other so splitting would either duplicate the palette or break cascade. | 2026-08-31 | dawid | docs/review/moderation-v2/MODERATION_SLICE_20_REPORTS_FOUNDATION_REPORT.md + server/domains-v2/moderation/__tests__/service.test.ts + server/domains-v2/moderation/__tests__/domain-contract.test.ts (registries + lifecycle + RBAC pins). | Backend files exceed 15-export + 240-line + 400-line budgets; frontend dialog/queue exceed 140-line component budget; CSS module exceeds 320-line module budget. Mitigated by behavior tests pinning every action transition + the policy/mapper extraction so the factory body is orchestration only + the target-registry capability gate blocks unsupported actions before action recording. Planned follow-up: once the live transport + per-target moderator action surfaces land, split the create-report flow out of `service.ts`, move the per-target capability registry to `target-registry.ts`, and break the queue page into list / detail subcomponents. | active | server/domains-v2/moderation/contracts.ts server/domains-v2/moderation/service.ts client/src/features-v2/moderation/types.ts client/src/features-v2/moderation/ReportDialog.tsx client/src/features-v2/moderation/ModerationQueuePage.tsx client/src/features-v2/moderation/Moderation.module.css |

## Expired / Revoked Exceptions

None.

## Rules

1. Exceptions must be approved by the project owner.
2. Exceptions must have an expiry date or condition.
3. Expired exceptions are automatically revoked.
4. Exceptions without evidence are invalid.
5. Broad exceptions (e.g. "all guards disabled") are forbidden.
6. Each exception applies to exactly one rule or a small named set.
