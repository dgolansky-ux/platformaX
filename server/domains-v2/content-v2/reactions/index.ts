/**
 * content-v2/reactions — barrel. Internal sub-domain; the public surface is
 * re-exported from content-v2/public-api.ts. Sub-barrel kept empty to keep
 * the index from leaking domain internals.
 */
export {};
