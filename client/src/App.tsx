export function App() {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        minHeight: "100vh",
        fontFamily: "system-ui, -apple-system, sans-serif",
        background: "#0a0a0a",
        color: "#fafafa",
      }}
    >
      <h1 style={{ fontSize: "2.5rem", fontWeight: 700, margin: 0 }}>
        PlatformaX V2 Foundation
      </h1>
      <p style={{ color: "#888", marginTop: "1rem" }}>
        Clean skeleton &mdash; ready for development
      </p>
    </div>
  );
}
