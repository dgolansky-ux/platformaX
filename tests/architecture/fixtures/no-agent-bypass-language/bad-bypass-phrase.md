# Red-case — planted bypass phrase outside policy prefixes

> Copy the phrase below into a non-governance path (e.g. `server/foo.ts`)
> to verify the guard fires.

```
temporary bypass
TEMPORARY_BYPASS
ALLOW_BYPASS
```
